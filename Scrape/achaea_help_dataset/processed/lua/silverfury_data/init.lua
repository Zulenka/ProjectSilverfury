-- Auto-generated by achaea_lua_exporter.py
local command_index = require("silverfury_data.command_index")
local ability_index = require("silverfury_data.ability_index")
local affliction_index = require("silverfury_data.affliction_index")
local defence_index = require("silverfury_data.defence_index")
local class_index = require("silverfury_data.class_index")
local skill_index = require("silverfury_data.skill_index")
local relations = require("silverfury_data.relations")
local command_names = require("silverfury_data.command_names")
local manifest = require("silverfury_data.manifest")

local M = {
  command_index = command_index,
  ability_index = ability_index,
  affliction_index = affliction_index,
  defence_index = defence_index,
  class_index = class_index,
  skill_index = skill_index,
  relations = relations,
  command_names = command_names,
  manifest = manifest,
}

return M
